import React, { useContext, useEffect } from 'react'

// import NoteContext from "../context/notes/noteContext"
const About = () => {
  // const context = useContext(NoteContext)
  // useEffect(() => {
  //    context.update();
  //     // eslint-disable-next-line
  // }, [])
  return (
    <div>

      Thiss  is my about page/.
    </div>
  )
}

export default About
